# RVM を有効にする。
# 以下に従って RVM に関連するファイルが必要。
# c.f. http://beginrescueend.com/rvm/install/

# Load RVM function
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"

# Local variables:
# mode: sh
# coding: euc-jp
# End:
